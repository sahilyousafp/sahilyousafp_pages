{
  "framework": "vite",
  "buildCommand": "cd site && npm install && npm run build",
  "outputDirectory": "site/dist",
  "rewrites": [
    { "source": "/sahilyousafp_pages/:path*", "destination": "/:path*" },
    { "source": "/sahilyousafp_pages", "destination": "/" }
  ]
}
